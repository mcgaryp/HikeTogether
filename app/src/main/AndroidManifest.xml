<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.e.hiketogether">

    <uses-permission android:name="android.permission.INTERNET" />                  //This lets us connect to the internet
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />    //Get General location
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />      //Get Specific location
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />      //This lets us ask if we're connected to internet

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Launcher">

        <activity android:name=".Views.Activities.NewSplashActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity android:name=".Views.Activities.MainActivity">

        </activity>
        <activity android:name=".Views.Activities.CreateAccountActivity" />
        <activity android:name=".Views.Activities.LoginActivity" />
    </application>

</manifest>