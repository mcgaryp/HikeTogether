<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.e.hiketogether">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/trail_placeholder"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/trail_placeholder"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Launcher">

        <activity android:name=".Views.Activities.newSplash">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity android:name=".Views.Activities.MainActivity">

        </activity>
        <activity android:name=".Views.Activities.CreateAccountActivity" />
        <activity android:name=".Views.Activities.LoginActivity" />
    </application>

</manifest>